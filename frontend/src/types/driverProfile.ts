export type driverActionStructure = {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  displayName: string;
  profilePic: string;
  homeAddress: string;
};

export type availabilityOptions = "ONLINE" | "OFFLINE" | "BUSY";
